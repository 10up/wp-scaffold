# patterns/

Block patterns and registration helper files in PHP.

## Purpose
- Provide reusable block pattern snippets for site editor insertion.
- Register patterns with WordPress. Example: `patterns/card.php`.

## How to add
- Create a new pattern PHP file with the WordPress HTML markup for the pattern.