import Navigation from '@10up/component-navigation';

const NavigationComponent = () => {
	// eslint-disable-next-line no-unused-vars
	const navigation = new Navigation('.primary-menu', {
		action: 'click',
	});
};

export default NavigationComponent;
